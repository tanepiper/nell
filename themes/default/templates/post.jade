.post
  h1= post.title

  .meta.alert.alert-info
    | Posted on #{plugins.dateformat(post.date, 'dddd, dS mmmm yyyy @ h:MMTT')} in categories #{post.tags.join(', ')}

  .main.well
    != post.content