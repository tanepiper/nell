.container-fluid
  .row-fluid
    .span4
      include sidebar-left

    .span8
      - if (post) {
        include post
      - } else if(posts) {
        - posts.forEach(function(post) {
          .post
            h1= post.title

            .main
              != post.content
        - });
      - }