extends default

block body
  .container-fluid
    .row-fluid
      .span8
        - if (posts) {
          - posts.forEach(function(post) {
            include post
          - });
        - }

      .span4
        include sidebar